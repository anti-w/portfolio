import * as Styled from './styles';

import { ProjectsCarousel } from '../ProjectsCarousel';

export const MyProjects = () => {
  return (
    <Styled.Container>
      <ProjectsCarousel />
    </Styled.Container>
  );
};
